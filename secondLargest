import ast 
str=ast.literal_eval(input())


def second_largest(str):
    m1, m2 = None, None
    for x in len(str):
        if x >= m1:
            m1, m2 = x, m1
        elif x > m2:
            m2 = x
    return m2

print(m2)
